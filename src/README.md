# src

Source code root for the application.

- `ui/`: React UI layer (views and presentation).
- `frontend/`: TypeScript client logic shared by UI components.
- `middleware/`: Cross-cutting request/response logic.
- `backend/`: Server-side APIs, business logic, and data access.

