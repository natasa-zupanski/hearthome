# domain/

Entities and interfaces — zero external imports.

Contains: entities (`User`, `Room`, `FurnitureItem`, `CurrencyWallet`) and repository/service interfaces (`RoomRepository`, `FitnessServiceClient`).

Also owns: field selection from the fitness service payload, and the calorie-to-currency conversion rule.
